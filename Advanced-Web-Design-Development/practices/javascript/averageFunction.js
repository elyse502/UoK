/*let num1 = Number(prompt("Enter the first number:"));
let num2 = Number(prompt("Enter the second number:"));
let num3 = Number(prompt("Enter the third number:"));

console.log(average(num1, num2, num3));
document.write(
  "The average of " +
    num1 +
    ", " +
    num2 +
    ", and " +
    num3 +
    " is: " +
    average(num1, num2, num3) +
    ".",
);

function average(num1, num2, num3) {
  return (num1 + num2 + num3) / 3;
}
*/

/*
let width = Number(prompt("Enter the width of the rectangle:"));
let length = Number(prompt("Enter the length of the rectangle:"));

const areaOfRectangle = (length, width) => length * width;

console.log(areaOfRectangle(length, width));
document.write(
  "The area of a rectangle with length " +
    length +
    " and width " +
    width +
    " is: " +
    areaOfRectangle(length, width) +
    ".",
);
*/

let products = ["Macbook Pro", "iPhone", "iPad", "Apple Watch", "AirPods"];
document.write(
  "The last product in the list is: " + products[products.length - 1] + ".",
);
